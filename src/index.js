import React from 'react';
import ReactDOM from 'react-dom/client';
import HoldingsTable from './Holding';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<HoldingsTable />);